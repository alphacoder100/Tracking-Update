# ── Core ─────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── Database ─────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30.0
pgvector>=0.3.0
alembic>=1.13.0

# ── Configuration ────────────────────────────────────────────
pydantic-settings>=2.0
python-dotenv>=1.0.0

# ── Computer Vision / ML (CPU) ───────────────────────────────
# torch / torchvision are installed CPU-only in the Dockerfile.
ultralytics>=8.2.0
insightface>=0.7.3
onnxruntime>=1.18.0
# Needed for the one-time YOLO .pt -> .onnx export at startup (YOLO_USE_ONNX).
onnx>=1.16.0
opencv-python-headless>=4.9.0
numpy>=1.26.0
Pillow>=10.0
